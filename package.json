{
  "name": "redmine-mcp-server",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.19.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/index.js",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration --no-file-parallelism",
    "test:e2e": "npm run build && npm run test:e2e:ci",
    "test:e2e:ci": "vitest run tests/e2e --no-file-parallelism",
    "context:measure": "npm run redmine:reset && npm run build && npm run context:measure:ci",
    "context:measure:ci": "COMPARE_CONTEXT_BASELINE=1 vitest run tests/e2e/context-measurement.test.ts tests/e2e/agent-brief-public-surface-context-budget.test.ts --reporter=verbose --no-file-parallelism",
    "context:baseline:update": "npm run redmine:reset && npm run build && UPDATE_CONTEXT_BASELINE=1 vitest run tests/e2e/context-measurement.test.ts tests/e2e/agent-brief-public-surface-context-budget.test.ts --reporter=verbose --no-file-parallelism",
    "ci:reproducibility": "bash scripts/ci/reproducibility.sh",
    "redmine:start": "docker compose -f docker/compose.yml up -d",
    "redmine:stop": "docker compose -f docker/compose.yml down",
    "redmine:seed:config": "docker compose -f docker/compose.yml exec -T redmine bundle exec rails runner /seed/config.rb",
    "redmine:seed:data": "docker compose -f docker/compose.yml exec -T redmine bundle exec rails runner /seed/data.rb",
    "redmine:seed": "npm run redmine:seed:config && npm run redmine:seed:data",
    "redmine:reset": "bash docker/scripts/reset-redmine.sh"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@modelcontextprotocol/client": "2.0.0",
    "@types/node": "24.3.0",
    "eslint": "10.8.1",
    "typescript": "6.0.3",
    "typescript-eslint": "8.67.0",
    "vitest": "3.2.4"
  }
}
