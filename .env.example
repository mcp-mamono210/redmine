# Docker Redmine test environment
POSTGRES_DB=redmine
POSTGRES_USER=redmine
POSTGRES_PASSWORD=redmine
REDMINE_PORT=3000

# MCP client connection settings for the local Docker test environment.
# This API key is deterministic and valid only for the seeded local/CI test Redmine.
# Never reuse this value in a production Redmine instance.
REDMINE_URL=http://localhost:3000
REDMINE_API_KEY=0123456789abcdef0123456789abcdef01234567
